# Installation
```bash
$ git clone https://github.com/vise890/dotfiles
$ cd dotfiles
$ make install
```

If you want to install the extra configs (e.g. ./hidpi):
```
stow -t ~ hidpi
```

## Pre-requisites
- `git`
- GNU `stow`
- GNU `make`
